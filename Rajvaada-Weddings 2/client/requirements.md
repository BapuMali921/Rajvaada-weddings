## Packages
framer-motion | Page animations and scroll reveals
lucide-react | Icons for UI elements
react-intersection-observer | For scroll-triggered animations if needed (though framer-motion handles most)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes

## Notes
Using Google Fonts: Playfair Display (Headings) and DM Sans (Body) via index.css
Images will be sourced from Unsplash with descriptive comments for stability.
Backend API for contact form is at POST /api/contact
